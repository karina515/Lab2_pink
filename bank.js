//


function CloseCardLimit() {
    let limit;
    limit = 10000
        for (let i=0; i==limit; i++) {
            if(i=limit){
                console.log('карта заблокированна')
            }
    }
    alert(' ') 
}